//By @Sun-XX
/*
#include "Item.hpp"

Laser::Laser(float init_x,float init_y,int length){ //≥ı ºªØº§π‚
	for(int i=0;i<length;i++){
		this->x_pos.push_back(init_x);
		this->y_pos.push_back(init_y);
		this->size.push_back(0);
	}
}

void Laser::move(){
	for(int i=0;i<this->head_velocity;i++){
		this->x_pos.push_back(this->x_pos.back()+SDL_cosf(angle)*);
	}
}
*/
