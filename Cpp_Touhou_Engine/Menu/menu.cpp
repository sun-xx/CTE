#include "menu/menu.hpp"
Menu::Menu(vector<vector<SDL_Texture*> > b_texs)
{
    vector<vector<SDL_Texture*> > p1;
    vector<SDL_Texture*> p2;
    vector<>
    for (p1 = a.begin(); p1 != a.end(); p1++)
	{
		for (p2 = p1->begin(); p2 != p1->end(); p2++)
		{
		}
	}
}
